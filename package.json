{
  "name": "prism",
  "version": "1.0.0",
  "description": "javascript h.264 decoder",
  "scripts": {
    "start": "browserify decoder.js > bundle_decoder.js && node server.js"
  },
  "dependencies": {
    "browserify": "^16.5.1",
    "pinipig": "^1.6.1"
  }
}
